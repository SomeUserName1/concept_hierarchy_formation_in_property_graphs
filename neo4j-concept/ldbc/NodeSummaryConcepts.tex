\begin{tikzpicture}[sibling distance=10em, every node/.style = {shape=rectangle, rounded corners, draw, align=center,top color=white, bottom color=blue!20}]]\node {Root}
	child { node {l0} 
		child { node {l00} }		child { node {l01} 
			child { node {l010} }			child { node {l011} }}}	child { node {l1} 
		child { node {l10} }		child { node {l11} }}	child { node {l2} }	child { node {l3} 
		child { node {l30} }		child { node {l31} 
			child { node {l310} }			child { node {l311} }}}	child { node {l4} 
		child { node {l40} 
			child { node {l400} 
}			child { node {l401} 
}			child { node {l402} 
}			child { node {l403} }			child { node {l404} }}		child { node {l41} 
			child { node {l410} }			child { node {l411} 
}			child { node {l412} }			child { node {l413} }}};
\end{tikzpicture}