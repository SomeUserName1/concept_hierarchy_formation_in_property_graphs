\begin{tikzpicture}[sibling distance=10em, every node/.style = {shape=rectangle, rounded corners, draw, align=center,top color=white, bottom color=blue!20}]]\node {Root}
	child { node {l0} 
		child { node {l00} }		child { node {l01} }		child { node {l02} }		child { node {l03} }		child { node {l04} }		child { node {l05} }		child { node {l06} }		child { node {l07} }		child { node {l08} }		child { node {l09} }		child { node {l010} }		child { node {l011} 
			child { node {l0110} 
}			child { node {l0111} 
}			child { node {l0112} }			child { node {l0113} }			child { node {l0114} }			child { node {l0115} }			child { node {l0116} }			child { node {l0117} }			child { node {l0118} }			child { node {l0119} }			child { node {l01110} }			child { node {l01111} }			child { node {l01112} }			child { node {l01113} }			child { node {l01114} }			child { node {l01115} }			child { node {l01116} }			child { node {l01117} }			child { node {l01118} }			child { node {l01119} }			child { node {l01120} }			child { node {l01121} }			child { node {l01122} }			child { node {l01123} }			child { node {l01124} }			child { node {l01125} }			child { node {l01126} }			child { node {l01127} }			child { node {l01128} }			child { node {l01129} }			child { node {l01130} }}		child { node {l012} }}	child { node {l1} 
		child { node {l10} }		child { node {l11} }		child { node {l12} }		child { node {l13} }		child { node {l14} }		child { node {l15} }		child { node {l16} }		child { node {l17} }		child { node {l18} }		child { node {l19} }		child { node {l110} }		child { node {l111} }		child { node {l112} }		child { node {l113} }		child { node {l114} }		child { node {l115} }		child { node {l116} }		child { node {l117} }		child { node {l118} }		child { node {l119} }		child { node {l120} }		child { node {l121} }		child { node {l122} }		child { node {l123} }		child { node {l124} }		child { node {l125} }		child { node {l126} }		child { node {l127} }		child { node {l128} }		child { node {l129} }		child { node {l130} }		child { node {l131} }		child { node {l132} }		child { node {l133} }		child { node {l134} 
			child { node {l1340} }			child { node {l1341} }			child { node {l1342} }			child { node {l1343} }			child { node {l1344} }			child { node {l1345} }			child { node {l1346} }			child { node {l1347} }			child { node {l1348} }			child { node {l1349} }			child { node {l13410} }			child { node {l13411} }			child { node {l13412} }			child { node {l13413} }			child { node {l13414} }			child { node {l13415} }			child { node {l13416} }			child { node {l13417} }			child { node {l13418} }			child { node {l13419} }			child { node {l13420} }			child { node {l13421} }			child { node {l13422} }			child { node {l13423} }			child { node {l13424} }			child { node {l13425} }			child { node {l13426} }			child { node {l13427} }			child { node {l13428} }			child { node {l13429} }			child { node {l13430} }			child { node {l13431} }			child { node {l13432} }			child { node {l13433} }			child { node {l13434} }			child { node {l13435} }			child { node {l13436} }			child { node {l13437} }			child { node {l13438} }			child { node {l13439} }			child { node {l13440} }			child { node {l13441} }			child { node {l13442} }			child { node {l13443} }			child { node {l13444} }			child { node {l13445} }			child { node {l13446} }			child { node {l13447} }			child { node {l13448} }			child { node {l13449} }}}	child { node {l2} 
		child { node {l20} 
			child { node {l200} }			child { node {l201} 
}			child { node {l202} }}		child { node {l21} 
			child { node {l210} }			child { node {l211} }			child { node {l212} }			child { node {l213} }			child { node {l214} 
}}		child { node {l22} 
			child { node {l220} }			child { node {l221} }			child { node {l222} }}}	child { node {l3} 
		child { node {l30} 
			child { node {l300} }			child { node {l301} }}		child { node {l31} 
			child { node {l310} 
}			child { node {l311} 
}			child { node {l312} }}		child { node {l32} }		child { node {l33} }		child { node {l34} }		child { node {l35} }		child { node {l36} }		child { node {l37} }		child { node {l38} }		child { node {l39} }		child { node {l310} }		child { node {l311} }		child { node {l312} }		child { node {l313} }		child { node {l314} }		child { node {l315} }		child { node {l316} }		child { node {l317} }		child { node {l318} }		child { node {l319} }		child { node {l320} }		child { node {l321} }		child { node {l322} }		child { node {l323} }		child { node {l324} }		child { node {l325} }		child { node {l326} }		child { node {l327} }		child { node {l328} }		child { node {l329} }		child { node {l330} }		child { node {l331} }		child { node {l332} }		child { node {l333} }		child { node {l334} }		child { node {l335} }		child { node {l336} }		child { node {l337} }		child { node {l338} }		child { node {l339} }		child { node {l340} }		child { node {l341} }		child { node {l342} }		child { node {l343} }		child { node {l344} }		child { node {l345} }		child { node {l346} }		child { node {l347} }		child { node {l348} }		child { node {l349} }		child { node {l350} }		child { node {l351} }		child { node {l352} }		child { node {l353} }		child { node {l354} }		child { node {l355} }		child { node {l356} }		child { node {l357} }		child { node {l358} }		child { node {l359} }		child { node {l360} }		child { node {l361} }		child { node {l362} }		child { node {l363} }		child { node {l364} }		child { node {l365} }		child { node {l366} }		child { node {l367} }};
\end{tikzpicture}