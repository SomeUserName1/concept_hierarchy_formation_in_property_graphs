\begin{tikzpicture}[sibling distance=10em, every node/.style = {shape=rectangle, rounded corners, draw, align=center,top color=white, bottom color=blue!20}]]\node {Root}
	child { node {l0} 
		child { node {l00} 
			child { node {l000} 
}			child { node {l001} }			child { node {l002} 
}}		child { node {l01} 
			child { node {l010} 
}			child { node {l011} 
}			child { node {l012} 
}			child { node {l013} 
}}		child { node {l02} 
			child { node {l020} }			child { node {l021} 
}			child { node {l022} 
}}		child { node {l03} }}	child { node {l1} 
		child { node {l10} 
			child { node {l100} }			child { node {l101} }}		child { node {l11} 
			child { node {l110} }			child { node {l111} }			child { node {l112} 
}}		child { node {l12} }		child { node {l13} 
			child { node {l130} 
}			child { node {l131} }			child { node {l132} }			child { node {l133} }			child { node {l134} }			child { node {l135} 
}}		child { node {l14} 
			child { node {l140} 
}			child { node {l141} }			child { node {l142} 
}}};
\end{tikzpicture}