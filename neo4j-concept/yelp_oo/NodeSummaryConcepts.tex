\begin{tikzpicture}[sibling distance=10em, every node/.style = {shape=rectangle, rounded corners, draw, align=center,top color=white, bottom color=blue!20}]]\node {Root}
	child { node {l0} 
		child { node {l00} }		child { node {l01} }		child { node {l02} }		child { node {l03} }		child { node {l04} }		child { node {l05} }		child { node {l06} }		child { node {l07} }		child { node {l08} }		child { node {l09} }}	child { node {l1} 
		child { node {l10} }		child { node {l11} }		child { node {l12} }		child { node {l13} }		child { node {l14} }		child { node {l15} }		child { node {l16} }		child { node {l17} }		child { node {l18} }		child { node {l19} }}	child { node {l2} 
		child { node {l20} }		child { node {l21} }		child { node {l22} }		child { node {l23} }		child { node {l24} }		child { node {l25} }		child { node {l26} }		child { node {l27} }		child { node {l28} }}	child { node {l3} 
		child { node {l30} 
			child { node {l300} }			child { node {l301} }			child { node {l302} }			child { node {l303} }			child { node {l304} }			child { node {l305} }			child { node {l306} }}		child { node {l31} 
			child { node {l310} }			child { node {l311} }			child { node {l312} }			child { node {l313} }			child { node {l314} }			child { node {l315} }}		child { node {l32} 
			child { node {l320} }			child { node {l321} }			child { node {l322} 
}			child { node {l323} 
}}};
\end{tikzpicture}