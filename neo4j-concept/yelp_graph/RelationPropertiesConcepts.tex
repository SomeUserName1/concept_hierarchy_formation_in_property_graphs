\begin{tikzpicture}[sibling distance=10em, every node/.style = {shape=rectangle, rounded corners, draw, align=center,top color=white, bottom color=blue!20}]]\node {Root}
	child { node {l0} 
		child { node {l00} }		child { node {l01} }		child { node {l02} 
			child { node {l020} 
}			child { node {l021} 
}			child { node {l022} }			child { node {l023} }			child { node {l024} }			child { node {l025} }			child { node {l026} }			child { node {l027} }			child { node {l028} }			child { node {l029} }			child { node {l0210} }			child { node {l0211} }			child { node {l0212} }			child { node {l0213} }			child { node {l0214} }			child { node {l0215} }			child { node {l0216} }			child { node {l0217} }			child { node {l0218} }			child { node {l0219} }			child { node {l0220} }			child { node {l0221} }			child { node {l0222} }			child { node {l0223} }			child { node {l0224} }			child { node {l0225} }			child { node {l0226} }			child { node {l0227} }			child { node {l0228} }			child { node {l0229} }			child { node {l0230} }			child { node {l0231} }			child { node {l0232} }			child { node {l0233} }			child { node {l0234} }			child { node {l0235} }			child { node {l0236} }			child { node {l0237} }			child { node {l0238} }			child { node {l0239} }			child { node {l0240} }			child { node {l0241} }			child { node {l0242} }			child { node {l0243} }			child { node {l0244} }}		child { node {l03} }}	child { node {l1} 
		child { node {l10} }		child { node {l11} }		child { node {l12} }		child { node {l13} }		child { node {l14} }		child { node {l15} }		child { node {l16} }		child { node {l17} }		child { node {l18} }		child { node {l19} }		child { node {l110} }		child { node {l111} }		child { node {l112} }		child { node {l113} }		child { node {l114} }		child { node {l115} }		child { node {l116} }		child { node {l117} }		child { node {l118} }		child { node {l119} }		child { node {l120} }		child { node {l121} }}	child { node {l2} 
		child { node {l20} 
			child { node {l200} }			child { node {l201} }			child { node {l202} }			child { node {l203} }			child { node {l204} }			child { node {l205} }			child { node {l206} }			child { node {l207} }			child { node {l208} }			child { node {l209} }			child { node {l2010} }			child { node {l2011} }			child { node {l2012} }			child { node {l2013} }			child { node {l2014} }			child { node {l2015} }			child { node {l2016} }			child { node {l2017} }			child { node {l2018} }			child { node {l2019} }}		child { node {l21} 
			child { node {l210} }			child { node {l211} }			child { node {l212} }			child { node {l213} }			child { node {l214} }			child { node {l215} }			child { node {l216} }			child { node {l217} }			child { node {l218} }			child { node {l219} }			child { node {l2110} }			child { node {l2111} }			child { node {l2112} }			child { node {l2113} }			child { node {l2114} }			child { node {l2115} }			child { node {l2116} }			child { node {l2117} }			child { node {l2118} }			child { node {l2119} }}		child { node {l22} 
			child { node {l220} 
}			child { node {l221} 
}			child { node {l222} 
}			child { node {l223} 
}}};
\end{tikzpicture}