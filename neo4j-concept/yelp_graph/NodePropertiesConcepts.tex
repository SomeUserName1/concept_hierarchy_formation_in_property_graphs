\begin{tikzpicture}[sibling distance=10em, every node/.style = {shape=rectangle, rounded corners, draw, align=center,top color=white, bottom color=blue!20}]]\node {Root}
	child { node {l0} }	child { node {l1} }	child { node {l2} }	child { node {l3} }	child { node {l4} 
		child { node {l40} }		child { node {l41} 
			child { node {l410} }			child { node {l411} }}}	child { node {l5} 
		child { node {l50} }		child { node {l51} }		child { node {l52} }		child { node {l53} }		child { node {l54} }		child { node {l55} }		child { node {l56} }		child { node {l57} }		child { node {l58} }		child { node {l59} }		child { node {l510} }		child { node {l511} }		child { node {l512} }		child { node {l513} }		child { node {l514} }		child { node {l515} }		child { node {l516} }		child { node {l517} }		child { node {l518} }		child { node {l519} }		child { node {l520} }		child { node {l521} }		child { node {l522} }		child { node {l523} }		child { node {l524} }		child { node {l525} }		child { node {l526} }		child { node {l527} }		child { node {l528} }		child { node {l529} }		child { node {l530} }		child { node {l531} }		child { node {l532} }		child { node {l533} }		child { node {l534} }		child { node {l535} }		child { node {l536} }		child { node {l537} }		child { node {l538} }		child { node {l539} }		child { node {l540} }		child { node {l541} }		child { node {l542} }		child { node {l543} }		child { node {l544} }		child { node {l545} }		child { node {l546} }		child { node {l547} }		child { node {l548} }		child { node {l549} }		child { node {l550} }		child { node {l551} }		child { node {l552} }		child { node {l553} }		child { node {l554} }		child { node {l555} }		child { node {l556} }		child { node {l557} }		child { node {l558} }		child { node {l559} }		child { node {l560} }		child { node {l561} }		child { node {l562} 
			child { node {l5620} }			child { node {l5621} }			child { node {l5622} 
				child { node {l56220} }				child { node {l56221} }}}}	child { node {l6} 
		child { node {l60} }		child { node {l61} }		child { node {l62} }		child { node {l63} }		child { node {l64} }		child { node {l65} }		child { node {l66} }		child { node {l67} }		child { node {l68} }		child { node {l69} }		child { node {l610} }		child { node {l611} }		child { node {l612} }		child { node {l613} }		child { node {l614} }		child { node {l615} }		child { node {l616} }		child { node {l617} }		child { node {l618} }		child { node {l619} }		child { node {l620} }		child { node {l621} }		child { node {l622} }		child { node {l623} 
			child { node {l6230} }			child { node {l6231} }			child { node {l6232} 
				child { node {l62320} }				child { node {l62321} }}}};
\end{tikzpicture}