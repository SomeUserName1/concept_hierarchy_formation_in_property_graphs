\begin{tikzpicture}[sibling distance=10em, every node/.style = {shape=rectangle, rounded corners, draw, align=center,top color=white, bottom color=blue!20}]]\node {Root}
	child { node {l0} 
		child { node {l00} 
			child { node {l000} }			child { node {l001} 
				child { node {l0010} }				child { node {l0011} 
}}}		child { node {l01} 
			child { node {l010} }			child { node {l011} }}		child { node {l02} 
			child { node {l020} }			child { node {l021} }}		child { node {l03} 
			child { node {l030} }			child { node {l031} 
				child { node {l0310} }				child { node {l0311} }}}		child { node {l04} 
			child { node {l040} }			child { node {l041} 
				child { node {l0410} }				child { node {l0411} }}}		child { node {l05} 
			child { node {l050} }			child { node {l051} }}		child { node {l06} 
			child { node {l060} }			child { node {l061} }}		child { node {l07} 
			child { node {l070} }			child { node {l071} }}		child { node {l08} 
			child { node {l080} }			child { node {l081} 
				child { node {l0810} }				child { node {l0811} }}}		child { node {l09} }		child { node {l010} }		child { node {l011} }		child { node {l012} }		child { node {l013} }		child { node {l014} 
			child { node {l0140} }			child { node {l0141} 
				child { node {l01410} }				child { node {l01411} }}}		child { node {l015} }		child { node {l016} 
			child { node {l0160} }			child { node {l0161} }}		child { node {l017} }		child { node {l018} 
			child { node {l0180} }			child { node {l0181} }}		child { node {l019} 
			child { node {l0190} 
				child { node {l01900} }				child { node {l01901} }				child { node {l01902} }}			child { node {l0191} }}		child { node {l020} 
			child { node {l0200} }			child { node {l0201} }}		child { node {l021} 
			child { node {l0210} }			child { node {l0211} }			child { node {l0212} 
				child { node {l02120} }				child { node {l02121} }}}}	child { node {l1} 
		child { node {l10} 
			child { node {l100} }			child { node {l101} }			child { node {l102} }			child { node {l103} 
				child { node {l1030} }				child { node {l1031} }}			child { node {l104} }			child { node {l105} 
				child { node {l1050} }				child { node {l1051} }}			child { node {l106} }			child { node {l107} 
				child { node {l1070} }				child { node {l1071} }}			child { node {l108} 
				child { node {l1080} }				child { node {l1081} }				child { node {l1082} }}			child { node {l109} }			child { node {l1010} }			child { node {l1011} 
				child { node {l10110} }				child { node {l10111} 
}}			child { node {l1012} 
				child { node {l10120} }				child { node {l10121} }				child { node {l10122} 
}				child { node {l10123} 
}}}		child { node {l11} 
			child { node {l110} }			child { node {l111} }			child { node {l112} }			child { node {l113} }			child { node {l114} }			child { node {l115} }			child { node {l116} }			child { node {l117} 
				child { node {l1170} }				child { node {l1171} }				child { node {l1172} 
}}}		child { node {l12} 
			child { node {l120} 
				child { node {l1200} }				child { node {l1201} }				child { node {l1202} 
}				child { node {l1203} 
}}			child { node {l121} }			child { node {l122} 
				child { node {l1220} 
}				child { node {l1221} }				child { node {l1222} 
}}}};
\end{tikzpicture}