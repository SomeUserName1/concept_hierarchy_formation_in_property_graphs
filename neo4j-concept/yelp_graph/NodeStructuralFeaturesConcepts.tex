\begin{tikzpicture}[sibling distance=10em, every node/.style = {shape=rectangle, rounded corners, draw, align=center,top color=white, bottom color=blue!20}]]\node {Root}
	child { node {l0} 
		child { node {l00} }		child { node {l01} }		child { node {l02} }}	child { node {l1} }	child { node {l2} }	child { node {l3} 
		child { node {l30} }		child { node {l31} 
			child { node {l310} }			child { node {l311} }			child { node {l312} }}}	child { node {l4} 
		child { node {l40} 
			child { node {l400} }			child { node {l401} }}		child { node {l41} 
			child { node {l410} }			child { node {l411} }}		child { node {l42} }		child { node {l43} }		child { node {l44} 
			child { node {l440} }			child { node {l441} }			child { node {l442} }}		child { node {l45} 
			child { node {l450} }			child { node {l451} }}};
\end{tikzpicture}