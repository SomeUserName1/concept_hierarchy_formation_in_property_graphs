\begin{tikzpicture}[sibling distance=10em, every node/.style = {shape=rectangle, rounded corners, draw, align=center,top color=white, bottom color=blue!20}]]\node {Root}
	child { node {l0} 
		child { node {l00} }		child { node {l01} 
			child { node {l010} }			child { node {l011} 
				child { node {l0110} }				child { node {l0111} }}}}	child { node {l1} 
		child { node {l10} }		child { node {l11} }		child { node {l12} 
			child { node {l120} }			child { node {l121} 
				child { node {l1210} }				child { node {l1211} }				child { node {l1212} }				child { node {l1213} }				child { node {l1214} 
}}			child { node {l122} 
				child { node {l1220} }				child { node {l1221} }				child { node {l1222} 
}				child { node {l1223} 
}				child { node {l1224} 
}}}		child { node {l13} }		child { node {l14} 
			child { node {l140} }			child { node {l141} 
				child { node {l1410} }				child { node {l1411} }				child { node {l1412} }				child { node {l1413} }				child { node {l1414} }				child { node {l1415} 
}}			child { node {l142} }			child { node {l143} }			child { node {l144} 
				child { node {l1440} 
}				child { node {l1441} 
}				child { node {l1442} 
}				child { node {l1443} }}}		child { node {l15} 
			child { node {l150} }			child { node {l151} }}}	child { node {l2} 
		child { node {l20} }		child { node {l21} }		child { node {l22} }		child { node {l23} }		child { node {l24} }		child { node {l25} 
			child { node {l250} }			child { node {l251} 
				child { node {l2510} }				child { node {l2511} }}}		child { node {l26} 
			child { node {l260} }			child { node {l261} 
				child { node {l2610} }				child { node {l2611} }}}};
\end{tikzpicture}